# Fixture-oracle cases (backlog #49): real `emerge -p` on the staged
# fixture tree, compared to portuale's own output. One case per line,
# shell words are emerge arguments. The set spans the resolver shapes
# the contract suite already covers, so each divergence is a real
# behavioural difference, not a new scenario.
#
# multi-package diamond
dev-libs/diamond
# the diamond leaf, whose staged cache entry is deliberately stale (#46
# S4): regenerated from the ebuild by both PMs, resolution unchanged
dev-libs/common
# || any-of group
dev-libs/anyof
# #74 S1: an installed || alternative's [use] dep is checked against its
# recorded vdb USE -- real keeps the installed altprov:1.1[flip] (vdb USE)
# over the uninstalled :1.0 slot, so the group contributes no merge
dev-libs/altconsumer
# #74 S2a: both installed slots of slotdedup are real nodes, so the deep
# walk reaches slot 2's own RDEPEND marker
-D dev-libs/slotdedupconsumer
# REQUIRED_USE with IUSE defaults
dev-libs/iusedefaultpkg
# multi-slot package
dev-libs/dualslotpkg
# installed pin vs an explicit upgrade
--update dev-libs/paired
# autounmask keyword change
dev-libs/autounmaskkeywordpkg
# keyword-masked dependency
dev-libs/kwneedpkg
# installed consumer's hard dependency breaks a pin
dev-libs/needer
# the same, with the competing othermod constraint present (real prints
# the installed-instance slot conflict in this shape; backlog #54/#57)
dev-libs/needer dev-libs/othermod
# ... and with the arguments the other way round: real's block is
# byte-identical, so portuale's must be argv-order-independent too (#57)
dev-libs/othermod dev-libs/needer
# #57 K2 regression guard: a bare `dev-libs/paired` dep plus needer's
# `>=paired-2.0` is a *solvable* same-slot installed-vs-merge collision --
# both PMs must merge the upgrade silently, in either argv order
dev-libs/plainuser dev-libs/needer
dev-libs/needer dev-libs/plainuser
# explicit request against an installed pin (same #54 shape)
=dev-libs/paired-2.0
# USE-dep blocker
dev-libs/blockusedeptarget
# unsatisfiable REQUIRED_USE
dev-libs/requiredusebadpkg
# #53: go-style || ( self other-bootstrap ) BDEPEND self-branch, with two
# hard BDEPEND consumers of the md2man-analogue -- portuale misorders the
# merge list (mogo after its owner); real resolves the || to itself
# (already merge-bound) and aborts with a circular-dependency error
# (known resolution difference, not a new portuale bug -- see L0 finding D)
app-misc/mopodman
# #53: nearest-passing pin -- same chain without the self-branch
app-misc/mopodmanok
# #77 A0: installed nomowner's vdb RDEPEND soft-blocks `<nomtarget-2.0`,
# nomowner is not walked (not in world, no dependency edge), and the
# explicit `=nomtarget-1.5` merges -- real uninstall-orders the owner and
# prints the satisfied row (n1)
=dev-libs/nomtarget-1.5
# #75 C3: the same shape in tree mode -- the satisfied row hangs under
# the removal (`[blocks b]`, `[uninstall]`, then the merge; C0 n5)
--tree =dev-libs/nomtarget-1.5
# #75 C3: the merging-owner removal row in tree mode --
# `[nomerge] blockerpkg`, `[blocks b]`, `[uninstall]`, `[ebuild N]`
--tree dev-libs/blockerpkg
# #75 C3: a plain tree (reversed-list layout, depth spaces)
--tree dev-libs/diamond
# #75 C3: the unordered DFS keeps the RDEPEND order
--tree --unordered-display dev-libs/treeorderpkg
# #82: a slot-unqualified atom matching two merge-bound slots must
# resolve to the vercmp-highest (1.10, SLOT 1), not the string-highest
# (1.9, SLOT 0) -- the tree nesting is what shows which one won
-v dev-libs/treeslotuser
--tree dev-libs/treeslotuser
# #82: the slot-blind-edge fixture in tree mode -- `slotorderdual:2` is
# slotorderroot's own dep, so the cp-keyed `required_by` fallback must
# not nest it under slotorderb instead
--tree dev-libs/slotorderroot
# #87 S2: the #76 B0 q3 RDEPEND control -- the same satisfied-replacement
# wait as q1's BDEPEND shape, but through an `RDEPEND="|| ( ... )"`
# group, which (unlike a BDEPEND) resolves against the target root and
# so survives the default bed's `ROOT=$FX` split. Real and portuale
# agree byte-for-byte here (host oracle 2026-09-18); this is the
# standing default-bed coverage for the `kept_alt_for_display` path
=dev-libs/rdisjtarget-2.0
# #81 S2: the tree-only satisfied row -- the replacement upgrades over an
# installed instance whose owner upgrades too, so flat mode's greedy pop
# drains past it with no row, but tree mode's one-at-a-time selection
# stalls and schedules the uninstall (`scheduled_uninstalls`), solving
# and showing the row. Real and portuale agree on the row bytes, the
# nesting and the -v counters (host oracle 2026-09-19); the surrounding
# walk sequence stays portuale's own (documented residue).
--tree --update dev-libs/p2btarget dev-libs/p2bowner dev-libs/newpkg
# #132 arm A: an installed parent built -flip evaluates its
# `[flip=]` RDEPEND atom to `[-flip]`; the installed child is +flip and
# the profile enables flip, so no pool satisfies it -- real's three-pool
# USE failure (the shrunk #111 live shape)
-D dev-libs/deepusedepconsumer
# #132 arm B: same parent shape, child not profile-pinned, so real
# rebuilds the child with flip off instead of aborting
-D dev-libs/deepusedepbconsumer
# #132 arm C: parent built +flip evaluates the same atom to `[flip]`,
# which the installed +flip child satisfies -- nothing moves
-D dev-libs/deepusedepokconsumer
# #132 arm D (S5b): installed parent built -flip, child flip
# profile-*forced* on, so `[-flip]` is unfixable -- real hard-aborts
# (the live shape: no autounmaskable pool, no Change USE rows)
-D dev-libs/deepusedepfconsumer
# #133: installed parent built -flip evaluates its `[flip=]` BDEPEND atom
# to `[-flip]`; the running root's installed child is +flip, so real
# resolves a running-root rebuild of the child with flip off instead of
# silently dropping the dep as satisfied (the --root-deps half of #132)
-D --root-deps dev-libs/deeprootdepconsumer
# #50 S1 batch 1 (upstream test_blocker pg0): A/B/C force X-1, whose
# RDEPEND `!=Y-1` uninstalls installed Y-1. The clean argv order guards
# #142's fixed point (real: X-1 + [uninstall]Y-1, no warning)
--backtrack=0 dev-libs/blk0a dev-libs/blk0c dev-libs/blk0b
# ... and a divergent order (uninstall pairs Y-2 + spurious conflict
# WARNING; #142, suppressed in known-divergences-fixture-oracle.yaml)
--backtrack=0 dev-libs/blk0a dev-libs/blk0b dev-libs/blk0c
# #50 S1 batch 1 (upstream test_blocker pg1, --buildpkgonly blockers):
# soft BDEPEND `!X` leaves installed X untouched (real merges C-1 bare;
# portuale spuriously uninstalls X — #143, suppressed)
--buildpkgonly dev-libs/blk1c
# ... and the hard DEPEND `!!X` abort (real rc 1; portuale merges +
# uninstalls, rc 0 — #143, suppressed)
--buildpkgonly dev-libs/blk1a
